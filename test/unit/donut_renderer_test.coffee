describe 'Donut renderer', ->
  it 'Donut renderer: initialize'#, ->
    #_donutRenderer = new window.Tactile.DonutRenderer()
    #assert _donutRenderer.initialize()

  it 'Donut renderer: render'#, ->
    #_donutRenderer = new window.Tactile.DonutRenderer()
    # assert _donutRenderer.render()
